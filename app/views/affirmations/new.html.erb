
<h1>Daily Affirmation</h1>


<%= form_with scope: :affirmation, url: affirmations_index_path, local: true do |form| %>
 
    <% if @affirmation.errors.any? %>
      <div id="error_explanation">
        <h2>
          <%= pluralize(@affirmation.errors.count, "error") %> prohibited
          this article from being saved:
        </h2>
        <ul>
          <% @affirmation.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
   
    <p>
      <%= form.label :text %><br>
      <%= form.text_area :text %>
    </p>
   
    <p>
      <%= form.submit %>
    </p>
   
  <% end %>
   
  <%= link_to 'Back', affirmations_index_path %>